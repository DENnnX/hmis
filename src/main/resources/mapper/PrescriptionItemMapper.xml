<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.scnu.hmis.mapper.PrescriptionItemMapper">

    <resultMap id="baseResultMap" type="cn.edu.scnu.hmis.entity.PrescriptionItem">
        <id column="id" property="id"/>
        <result column="prescription_id" property="prescriptionId"/>
        <result column="drug_id" property="drugId"/>
        <result column="quantity" property="quantity"/>
        <result column="dosage" property="dosage"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="subtotal" property="subtotal"/>
    </resultMap>

    <select id="findAll" resultMap="baseResultMap">
        SELECT id, prescription_id, drug_id, quantity, dosage, unit_price, subtotal
        FROM prescription_item
    </select>

    <select id="findById" resultMap="baseResultMap">
        SELECT id, prescription_id, drug_id, quantity, dosage, unit_price, subtotal
        FROM prescription_item
        WHERE id = #{id}
    </select>

    <select id="findByPrescriptionId" resultMap="baseResultMap">
        SELECT id, prescription_id, drug_id, quantity, dosage, unit_price, subtotal
        FROM prescription_item
        WHERE prescription_id = #{prescriptionId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO prescription_item (prescription_id, drug_id, quantity, dosage, unit_price, subtotal)
        VALUES (#{prescriptionId}, #{drugId}, #{quantity}, #{dosage}, #{unitPrice}, #{subtotal})
    </insert>

</mapper>
